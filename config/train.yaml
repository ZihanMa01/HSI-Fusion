# 训练配置
# 用法: python run.py --mode train --config config/train.yaml

defaults:
  - default.yaml

experiment:
  name: "train_experiment"

training:
  epochs: 200
  log_interval: 50

  optimizer:
    type: "adam"
    lr: 1.0e-4

  scheduler:
    type: "cosine"
    eta_min: 1.0e-6
