# HSI-MSI Fusion Benchmark Framework 依赖

# 核心
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.22
scipy>=1.8

# 配置
pyyaml>=6.0
omegaconf>=2.3

# 评估
scikit-image>=0.20
thop>=0.1        # FLOPs 计算 (可选)

# 可视化
matplotlib>=3.6
seaborn>=0.12     # (可选) 增强可视化

# 训练辅助
tensorboard>=2.12
tqdm>=4.64

# 开发 / 测试
pytest>=7.0
